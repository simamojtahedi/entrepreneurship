import React from 'react'
import './style.scss'

const BoxTitle = (props) => {
    return (
        <div className="BoxTitle">
            {props.title}
        </div>
    )
}

export default BoxTitle
